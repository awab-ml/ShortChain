## Summary

<!-- One or two sentences: what this changes and why. -->

## Changes

<!-- Bullet list of the concrete changes. -->

## Testing

- [ ] `pytest tests/ -q` passes
- [ ] `ruff check shortchain/ tests/ scripts/` is clean
- [ ] Relevant per-suite tests pass (`pytest tests/<module>/`)

## Checklist

- [ ] No generated artifacts (`data/`, `models/`) are committed
- [ ] No experiment write-ups added to `docs/`
- [ ] Tests mirror the package layout (`tests/<module>/`)